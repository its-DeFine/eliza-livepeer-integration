import conductor from './conductor';
import synthesiser from './synthesiser';
import narrator from './narrator';

// Export the team as an array
export const vtuberTeam = [conductor, synthesiser, narrator];

export default vtuberTeam;
